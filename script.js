function darkMode() {
    var theme = "dark-mode";
    document.getElementById("body").className = theme;
}

function lightMode() {
    var theme = "light-mode";
    document.getElementById("body").className = theme;
}
